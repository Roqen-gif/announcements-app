# Server
PORT=3000
NODE_ENV=development

# Database (PostgreSQL connection string)
DATABASE_URL=postgresql://user:password@localhost:5432/announcements_db?schema=public

# JWT / Auth (якщо плануєш додати)
JWT_SECRET=your_jwt_secret_key_here

# Logging
LOG_LEVEL=info